import React from 'react';
import Icon from '../components/icon';

const Header = () => (
  <header className="mb-5 mb-6-s">
    <a href="/" className="h-fade">
      <Icon name="zigzag" size={28} iconSize={28} />
    </a>
  </header>
);

export default Header;
