{% extends 'layout/base' %}

{# `Title — Site` or just `Site` #}
{% block title -%}
	{% if page.title -%}
		{{ page.title }} &mdash; 
	{%- endif -%}
	{% parent %}
{%- endblock %}

{% block body %}
	<main>
		{% block content %}
		{% endblock %}
	</main>
	{% parent %}
{% endblock %}