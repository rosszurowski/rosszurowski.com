#!/usr/bin/env node

const hibiscss = require('hibiscss').default;
const kit = require('hibiscss/default');

const theme = require('../src/lib/theme');

const breakpoints = {
  s: '479px',
  m: '959px',
};

const styles = hibiscss(kit(theme), breakpoints);

console.log(styles);
